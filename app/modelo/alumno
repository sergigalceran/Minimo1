var mongoose = require('mongoose');

module.exports = mongoose.model('Alumno',{
    nombre: String,
    direccion: String,
    telefono: {
        fijo:Number,
        movil:Number
            },
    estudios:[]
});